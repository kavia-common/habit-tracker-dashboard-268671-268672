/**
 * @fileoverview CRA web vitals reporting (unused in this project).
 */

/**
 * PUBLIC_INTERFACE
 * @param {(Function|undefined)} onPerfEntry
 * @return {void}
 */
export function reportWebVitals(onPerfEntry) {
    // Intentionally left blank; metrics can be wired later if needed.
    void onPerfEntry;
}
